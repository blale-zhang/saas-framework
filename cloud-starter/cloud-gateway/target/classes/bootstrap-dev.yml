saas:
  ip: 127.0.0.1
  server-center:
    ip: ${saas.ip}
    port: 8761
    url: http://${saas.server-center.ip}:${saas.server-center.port}
    enabled: true
  admin:
    ip: ${saas.ip}
    port: 6789
    url: http://${saas.admin.ip}:${saas.admin.port}

spring:
  redis: #Redis
    host: 47.106.248.186
    port: 6379
    database: 5  #数据库索引
    password: blade123 #密码
    timeout: 60000ms  #连接超时时间
    lettuce: # 这里标明使用lettuce配置
      pool:
        max-active: 10   #连接池最大连接数（使用负值表示没有限制）
        max-wait: -1ms  #连接池最大阻塞等待时间（使用负值表示没有限制）
        max-idle: 5     #连接池中的最大空闲连接
        min-idle: 0     #连接池中的最小空闲连接


smr:
  uri:
    exclude: /login/login,/login/login3,/login/loginOut